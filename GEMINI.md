# Project Instructions

- All control structures must use braces `{}`, even if they contain only a single statement.
- Follow Allman style (braces on new lines) for all blocks.
- Run `clasp push` after every turn to keep the remote deployment synchronized.
